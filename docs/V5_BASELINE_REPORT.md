# V5 Verified Baseline

## Repository facts

- Baseline: V4 complete archive.
- Files: **446**.
- Packages/apps with manifests: **19**.
- Next.js pages: **54**.
- API routes: **11**.
- Forward migrations: **22** (`0001` through `0022`).
- Existing architecture: Next.js modular monolith, PostgreSQL authority/RLS, pure exact-genetics core, durable Node worker, bounded Python worker, source-backed catalog, immutable scientific snapshots.

## Runtime availability

- Repository requires Node 24 and pnpm 10.14.0.
- Default shell exposes Node 22.16.0.
- pnpm is not installed.
- npm-registry DNS is unavailable.
- PostgreSQL, Docker, and R runtimes are unavailable.
- No lockfile will be invented or hand-authored.

## Confirmed implementation gaps

1. Advanced engines are implemented as pure functions but are not unified behind one governed persisted laboratory.
2. Research review/retrieval exists, but durable source ingestion and deterministic passage extraction are inactive.
3. Media quarantine/inspection exists, but immutable derivatives, metadata-stripped outputs, deterministic measurements, and durable Python participation are incomplete.
4. Breeding-ledger export obtains one database JSON snapshot and serializes it in memory.
5. Hosted AI remains intentionally inactive without approved provider/privacy/cost controls.
6. MIAPPE conformance, complete E2E/accessibility, PostgreSQL authority execution, Compose, and restore proof remain unavailable.

## V5 execution order

1. Add forward-only authority migrations and versioned contracts.
2. Implement canonical application services and immutable snapshots.
3. Add durable worker handlers with lease/cancellation checks.
4. Complete browser workflows without direct authority writes.
5. Add scientific/runtime fixtures and upgrade the behavioral validator.
6. Produce machine-readable evidence, file ledger, checksums, and archives.
