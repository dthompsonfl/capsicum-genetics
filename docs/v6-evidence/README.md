# V6 local validation evidence

These files record commands executed on the local validation host. They are useful source-level evidence, but they are **not** a substitute for commit-bound CI evidence from a clean frozen install, PostgreSQL 18, MinIO/ClamAV, browsers, containers, and an isolated restore environment. The release evidence generator excludes this directory from the implementation source-tree hash.
