# V4 Release Blockers

The release verdict remains **BLOCKED** for the following genuine reasons.

## Environment/runtime proof

1. Registry access failed; no legitimate `pnpm-lock.yaml` or frozen install exists.
2. Node 24 and repository-pinned TypeScript 5.9.2 were unavailable.
3. PostgreSQL 18 migration replay, restricted-role RLS/authority, concurrency, and query-plan tests were not executed.
4. Docker/Compose, MinIO, ClamAV, worker crash/restart, and object-storage integration were not executed.
5. Playwright, axe, keyboard, responsive, and manual nontechnical-user journeys were not executed.
6. Backup and isolated restore reconciliation were not executed.
7. Dependency, license, secret, SAST, container scans, and SBOM were not executed.
8. R runtime was unavailable.

## Remaining implementation work

1. Unify linkage, maternal, conditional-rule, host-pathogen, and direct Monte Carlo execution through one persisted governed laboratory.
2. Activate durable scientific-document ingestion and passage extraction with reviewed extraction artifacts.
3. Complete media derivative generation, metadata stripping, deterministic measurements, and Python-worker durable participation.
4. Replace in-memory breeding-ledger export assembly with bounded streaming normalized artifacts.
5. Complete hosted-AI provider privacy/cost/time/token controls and adversarial evaluations before activation.
6. Add MIAPPE conformance fixtures and mappings before claiming compatibility.
7. Complete authenticated E2E coverage for every required acceptance journey.

## Scientific/data blockers

- No independently approved executable phenotype rules exist.
- No independently validated learned vision or quantitative prediction model exists.
- Real catalog claims still require domain-expert scientific review before operational reliance.
